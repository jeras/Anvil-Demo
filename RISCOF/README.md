To prepare the required tools for running RISCOF execute:
```
./setup.sh
```
This setup does not include Verilator, it must be setup (installed) separately.

To execute the RISCOF tests run:
```
.build.sh
```

Check the folder for each DUT test for the `*.fst` waveform file.